# Core web framework — pure JSON API, consumed by the separate React/Vite app
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Database
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
alembic==1.13.2

# Config / validation
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# Auth — decoupled frontend means token-based auth (JWT bearer), not
# server-rendered sessions
passlib==1.7.4
# bcrypt==3.2.2
python-jose[cryptography]==3.3.0
python-multipart==0.0.9

# Stats for outlier detection
numpy==1.26.4

# Ollama is accessed over local HTTP (ollama:11434 inside Docker network) —
# plain `requests` keeps this to one dependency instead of an SDK.
requests==2.32.3

# Testing
pytest==8.3.3
httpx==0.27.2
